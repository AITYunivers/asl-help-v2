using LiveSplit.ComponentUtil;

public partial class Basic
{
    public bool TryReadString(out string result, int length, ReadStringType stringType, nint address, params int[] offsets)
    {
        result = null;
        return true;
    }
}
